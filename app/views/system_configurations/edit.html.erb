<h1>System Configuration Key: <%= @system_configuration.key %></h1>

<%= simple_form_for(@system_configuration, data: { turbo: false }) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="row">
    <div class="col-6 col-md-8 col-sm-12">
      <div class="form-inputs">
        <%= f.input :value, as: :text %>
      </div>
    </div>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Submit', class: 'btn btn-primary' %>
  </div>
<% end %>

<div class="pt-3">
 <%= link_to "Back to system settings", system_settings_path %>
</div>
