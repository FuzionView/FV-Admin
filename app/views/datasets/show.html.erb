<p style="color: green"><%= notice %></p>

<%= render @dataset %>

<div>
  <%= link_to "Edit this dataset", edit_dataset_path(@dataset) %> |
  <%= link_to "Back to owner", owner_path(@dataset.owner) %>

  <%= button_to "Destroy this dataset", @dataset, method: :delete %>
</div>
